# Copy this file to .streamlit/secrets.toml (locally) or paste into
# "Secrets" in the Streamlit Community Cloud app settings.
# Optional — only needed if you want Claude-powered extraction instead of
# the built-in regex engine.

ANTHROPIC_API_KEY = "sk-ant-..."
